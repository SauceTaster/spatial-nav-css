---
'spatial-nav-css': minor
---

Add a no-build CDN path and LLM-oriented documentation:

- Ship a minified IIFE bundle (`dist/spatial-nav.global.js`, `window.SpatialNav`)
  with the framework-free surface — core, custom elements, dialogs,
  virtual-list helper, and debug overlay — wired to the `unpkg`/`jsdelivr`
  package fields for one-line `<script>` usage.
- Ship `llms.txt` (curated, LLM-oriented API index) and `llms-full.txt`
  (complete docs in one file, generated by `npm run build:llms` and checked
  for freshness in CI) in the npm package.
